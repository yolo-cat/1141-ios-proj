# ESP32 (Arduino)

Stage-1 firmware workspace for sending DHT11 readings to Supabase.

- 需求與守則：參考 `AGENTS.md`。
- 可執行步驟：依照 `DEV_STEPS.md`（GPIO 15、5 分鐘周期，Demo 10 秒）。
- 草稿範例：`arduino_draft.ino`。
- 機密範本：`secrets.h.template`（自建 `secrets.h` 並加入 `.gitignore`）。
